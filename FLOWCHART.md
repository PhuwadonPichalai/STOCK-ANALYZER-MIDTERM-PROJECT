# Flowchart การทำงานของโปรเจ็กต์ S&P 500 Stock Analyzer

แผนภาพนี้อ้างอิงการทำงานจริงใน `download_data.py` และ `main.py` โดยใช้ Mermaid สามารถเปิดดูในโปรแกรมที่รองรับ Mermaid หรือคัดลอกโค้ดแผนภาพไปใช้ต่อได้

## 1. ภาพรวมของโปรเจ็กต์

```mermaid
flowchart TD
    A([เริ่มต้นใช้งาน]) --> B{มี CSV ที่ต้องการใช้แล้วหรือไม่?}
    B -- ไม่มี หรือต้องการอัปเดต --> C[ผู้ใช้รัน download_data.py]
    C --> D[ดึงรายชื่อหุ้น S&P 500 จาก Wikipedia]
    D --> E[ดึงข้อมูลหุ้นผ่าน yfinance และคำนวณ Score]
    E --> F[(บันทึก sp500_fundamentals.csv)]
    B -- มีแล้ว --> G[ผู้ใช้รัน main.py]
    F --> G
    G --> H[อ่าน CSV เป็น List สองมิติ]
    H --> I[ใช้ Merge Sort เรียง Score จากมากไปน้อย]
    I --> J[/แสดงหุ้นคะแนนสูงสุดไม่เกิน 5 บริษัท และเวลาจัดเรียง/]
    J --> K([จบการทำงาน])
```

แผนภาพนี้เป็นลำดับการใช้งาน: ทั้งสองสคริปต์ต้องรันแยกกัน `main.py` ไม่ได้เรียก `download_data.py` อัตโนมัติ และสามารถใช้ไฟล์ CSV ที่มีอยู่ได้ทันที

## 2. การเตรียมข้อมูล — download_data.py

```mermaid
flowchart TD
    A([เริ่ม main]) --> B[เรียก get_sp500_tickers]
    B --> C[ส่ง HTTP GET ไป Wikipedia พร้อม User-Agent]
    C --> D[อ่านตารางแรกด้วย pandas.read_html]
    D --> E[นำคอลัมน์ Symbol เป็น List และแทนจุดด้วยขีดกลาง]
    E --> F[คืนรายชื่อหุ้น และแสดงจำนวนที่พบ]
    F --> G[เปิด CSV โหมด w และเขียน Header]
    G --> H[กำหนด count = 0 และเริ่มจับเวลา]
    H --> I{ยังมีหุ้นตัวถัดไปหรือไม่?}
    I -- มี --> J[อ่าน stock.info ผ่าน yfinance]
    J --> L[อ่าน ROA, Margin และ debtToEquity]
    L --> M[แปลง D/E: หาร 100 หากมีค่า มิฉะนั้นใช้ 0.0]
    M --> N{ROA, Margin หรือ D/E เป็น None หรือไม่?}
    N -- ใช่ --> I
    N -- ไม่ใช่ --> O[Score = ROA × 0.4 + Margin × 0.4 - D/E × 0.2]
    O --> P[เขียน Ticker, Score, ROA, Margin, D/E ลง CSV]
    P --> Q[เพิ่ม count อีก 1]
    Q --> R{count หาร 50 ลงตัวหรือไม่?}
    R -- ใช่ --> S[/แสดงจำนวนที่บันทึกแล้ว/]
    S --> I
    R -- ไม่ใช่ --> I
    J -. เกิด Exception ภายใน try ของหุ้นตัวนี้ .-> X[ข้ามหุ้นตัวนี้ด้วย continue]
    X --> I
    I -- ไม่มี --> T[ออกจาก with และปิดไฟล์ CSV]
    T --> U[/แสดงจำนวนที่บันทึก ชื่อไฟล์ และเวลาที่ใช้/]
    U --> V([จบการทำงาน])
```

- `try/except` ครอบคลุมตั้งแต่ดึงข้อมูลของหุ้นแต่ละตัวจนถึงแสดงความคืบหน้า หากเกิด Exception ในช่วงนี้จะข้ามไปหุ้นถัดไป
- หากไม่มีคีย์ ROA หรือ Margin จะใช้ `0` แต่หากมีคีย์และค่าเป็น `None` จะข้ามหุ้นนั้น ส่วน D/E ที่ไม่มีค่าจะถูกแทนด้วย `0.0`
- การดึงรายชื่อจาก Wikipedia และการเปิดไฟล์อยู่นอก `try/except` ดังกล่าว หากล้มเหลว โปรแกรมจะหยุดด้วยข้อผิดพลาด
- โหมด `w` เขียนทับไฟล์ CSV เดิมเมื่อรันสคริปต์นี้

## 3. โปรแกรมหลัก — main.py

```mermaid
flowchart TD
    A([เริ่ม main]) --> B[กำหนดชื่อไฟล์ sp500_fundamentals.csv]
    B --> C[เรียก readRecordToList]
    C --> D{เปิดไฟล์ได้หรือไม่?}
    D -- ไม่พบไฟล์: FileNotFoundError --> E[/แจ้งให้รัน download_data.py ก่อน/]
    E --> Z([จบการทำงาน])
    D -- ได้ --> F[สร้าง List ว่าง และข้าม Header]
    F --> G{ยังมีแถวข้อมูลหรือไม่?}
    G -- มี --> H[อ่าน Ticker และแปลง Score, ROA, Margin, D/E เป็น float]
    H --> I[เพิ่ม Record ลง List สองมิติ]
    I --> G
    G -- ไม่มี --> J[ปิดไฟล์และคืน data_list]
    J --> K[/แสดงจำนวนบริษัทที่อ่านได้/]
    K --> L[กำหนด key_column = 1 ซึ่งเป็นคอลัมน์ Score]
    L --> M[แสดงสถานะจัดเรียง และเริ่มจับเวลา]
    M --> N[เรียก merge_sort_rec เพื่อเรียงจากมากไปน้อย]
    N --> O[/แสดงเวลาจัดเรียง และหัวตาราง Top 5/]
    O --> P[กำหนด i = 0]
    P --> Q{"i < min(5, จำนวนหุ้นที่เรียงแล้ว)?"}
    Q -- ใช่ --> R[อ่านหุ้นอันดับ i และจัดรูปแบบตัวเลข]
    R --> S[/แสดงอันดับ ชื่อหุ้น ROA, Margin, D/E และ Score/]
    S --> T[เพิ่ม i อีก 1]
    T --> Q
    Q -- ไม่ใช่ --> U[/แสดงเส้นปิดตาราง/]
    U --> Z
```

Record แต่ละแถวมีรูปแบบ `[Ticker, Score, ROA, Margin, Debt_to_Equity]` โดยใช้คอลัมน์ index `1` เป็นกุญแจจัดเรียง ตอนแสดงผลจะคูณ ROA และ Margin ด้วย 100 เพื่อแสดงเป็นเปอร์เซ็นต์

ในโค้ดจริง การแสดงผลใช้ `for i in range(min(5, len(sorted_stocks)))` แผนภาพจึงแสดงเป็นการตรวจเงื่อนไขและเพิ่ม `i` เพื่อให้เห็นการวนซ้ำชัดเจน หากข้อมูล CSV ผิดรูปแบบหรือแปลงเป็น `float` ไม่ได้ โปรแกรมไม่ได้ดักข้อผิดพลาดนั้นไว้

## 4. การแบ่งข้อมูลแบบเรียกซ้ำ — merge_sort_rec(arr, key_col)

```mermaid
flowchart TD
    A([รับ arr และ key_col]) --> B{"len(arr) <= 1?"}
    B -- ใช่ --> C[คืน arr เดิม]
    C --> Z([จบการเรียกครั้งนี้])
    B -- ไม่ใช่ --> D[คำนวณ mid = len arr หาร 2 ปัดลง]
    D --> E[เรียก merge_sort_rec กับครึ่งซ้าย และเก็บผลใน left]
    E --> F[เรียก merge_sort_rec กับครึ่งขวา และเก็บผลใน right]
    F --> G[เรียก merge กับ left, right และ key_col]
    G --> H[คืน List ที่รวมและเรียงแล้ว]
    H --> Z
```

การเรียกซ้ำแต่ละครั้งจะใช้ขั้นตอนเดียวกัน จนเหลือ List ที่มี 0 หรือ 1 รายการ จากนั้นจึงรวมผลกลับขึ้นมาตามลำดับการเรียก

## 5. การรวมข้อมูล — merge(left, right, key_col)

```mermaid
flowchart TD
    A([รับ left, right และ key_col]) --> B[สร้าง result ว่าง และกำหนด i = j = 0]
    B --> C{"i < len(left) และ j < len(right)?"}
    C -- ใช่ --> D{"left[i][key_col] >= right[j][key_col]?"}
    D -- ใช่ --> E[เพิ่ม left ที่ตำแหน่ง i ลง result]
    E --> F[เพิ่ม i อีก 1]
    F --> C
    D -- ไม่ใช่ --> G[เพิ่ม right ที่ตำแหน่ง j ลง result]
    G --> H[เพิ่ม j อีก 1]
    H --> C
    C -- ไม่ใช่ --> I[ต่อข้อมูลฝั่ง left ที่เหลือตั้งแต่ตำแหน่ง i ลง result]
    I --> J[ต่อข้อมูลฝั่ง right ที่เหลือตั้งแต่ตำแหน่ง j ลง result]
    J --> K[คืน result]
    K --> Z([จบการทำงาน])
```

เงื่อนไข `>=` ทำให้เลือกคะแนนมากกว่าก่อน จึงเรียงจากมากไปน้อย หากคะแนนเท่ากันจะเลือกฝั่งซ้ายก่อน ทำให้คงลำดับเดิมของรายการที่คะแนนเท่ากัน

สัญลักษณ์: วงรี = เริ่ม/จบ, สี่เหลี่ยม = ประมวลผล, สี่เหลี่ยมข้าวหลามตัด = เงื่อนไข, สี่เหลี่ยมด้านขนาน = แสดงผล และทรงกระบอก = ไฟล์ข้อมูล
